# CycloneNet – Forensic Atmospheric Analysis Framework
# Python 3.10+ required (codebase uses PEP 604 union syntax `X | Y`); 3.12 / 3.14 tested.
# All dependencies are listed with minimum versions; use pip install -r requirements.txt

# ------------------------------
# Core scientific libraries
# ------------------------------
numpy>=1.21.0
pandas>=1.3.0
xarray>=2022.3.0
netCDF4>=1.5.8
h5netcdf>=1.0.0
scipy>=1.7.0
scikit-learn>=1.0.0

# ------------------------------
# Geospatial calculations
# ------------------------------
geopy>=2.2.0                 # Great-circle distance (haversine)

# Optional geospatial plotting (used in evaluation notebooks)
# cartopy>=0.20.0             # Map projections and coastlines
# matplotlib>=3.4.0            # Already used for plotting; keep if needed
# seaborn>=0.11.0              # Optional styling

# ------------------------------
# Data download and APIs
# ------------------------------
cdsapi>=0.6.0                # Copernicus Climate Data Store (ERA5)
requests>=2.26.0              # IBTrACS download
python-dotenv>=0.19.0         # Environment variable management (optional)

# ------------------------------
# Progress bars and configuration
# ------------------------------
tqdm>=4.62.0
PyYAML>=6.0                   # YAML configuration parsing

# ------------------------------
# Deep learning framework
# ------------------------------
torch>=1.10.0                 # PyTorch (CUDA support recommended for training)
# torchvision not required

# ------------------------------
# Development / testing
# ------------------------------
pytest>=7.0                   # Unit tests under tests/ (physics, geometry, splits, losses)

# ------------------------------
# MCP server (mcp_server.py)
# ------------------------------
mcp>=1.0                      # Model Context Protocol SDK (FastMCP)

# ------------------------------
# Additional utilities (if needed)
# ------------------------------
# ftplib is part of the standard library, no installation required


matplotlib>=3.5.0