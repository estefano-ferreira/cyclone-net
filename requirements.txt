# CycloneNet V2.1 - Scientific Version
# Required Python packages for the full pipeline
# Python 3.9 or 3.10 recommended

# Core scientific libraries
numpy>=1.21.0
pandas>=1.3.0
xarray>=2022.3.0
netCDF4>=1.5.8
h5netcdf>=1.0.0
scipy>=1.7.0
scikit-learn>=1.0.0

# Geospatial calculations
geopy>=2.2.0

# Data download and API
cdsapi>=0.6.0
requests>=2.26.0
python-dotenv>=0.19.0

# Progress bars and utilities
tqdm>=4.62.0
PyYAML>=6.0

# Visualization (optional for plotting, but used in evaluation)
matplotlib>=3.4.0
seaborn>=0.11.0
cartopy>=0.20.0          # optional, used for storm track plots

# Deep learning framework
torch>=1.10.0             # PyTorch with CUDA support recommended
# torchvision not required, but can be installed if needed

pip install geopy netCDF4 scipy